apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: accommodation-2306223206-fe-ingress
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
    - host: fe.apap.hafizmuh.site
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: accommodation-2306223206-fe-service
                port:
                  number: 80