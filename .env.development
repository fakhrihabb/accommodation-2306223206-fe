# Development environment variables (localhost)
# Backend API base URL - using production backend
VITE_API_URL=http://2306223206-be.hafizmuh.site

# Authentication Configuration
# Use localStorage + URL token passing in development (cookies don't work on localhost)
VITE_USE_COOKIE_AUTH=false

# Centralized auth service URL
# For development, using same port since login/register are in this app
VITE_AUTH_DOMAIN=http://localhost:5173
