# Local development environment variables
# Backend API base URL (without /api suffix)
# Using production backend
VITE_API_URL=http://2306223206-be.hafizmuh.site

# Authentication Configuration
# Use cookie-based auth (true) or localStorage (false)
VITE_USE_COOKIE_AUTH=true

# Cookie domain (include leading dot for subdomain sharing)
VITE_COOKIE_DOMAIN=.hafizmuh.site

# Centralized auth service URL
VITE_AUTH_DOMAIN=https://auth.hafizmuh.site
